- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc: 
     ~/.gitconfig:

- create:
    - ~/me

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
